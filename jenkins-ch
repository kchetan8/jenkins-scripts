pipeline {
    agent any
 stages {
     stage('checkout') {
        steps {
            bat 'git clone %git-repo-url%'
        }
    }
    stage('checkout') {
        steps {
            bat '''cd %git-repo-name%
                    mvn clean install'''
        }
    }
 }
}

