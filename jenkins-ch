pipeline {
    agent any
 stages {
     stage('checkout') {
        steps {
            bat '''echo %cd%
                   echo %git-repo-name%
                   echo %git-repo-url%
                   echo %WORKSPACE%'''
        }
    }
 }
}

